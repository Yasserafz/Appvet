import 'package:flutter/material.dart';

// Définir les couleurs utilisées
const Color profileAppBarColor =
    Color(0xFF6200EA); // Exemple de couleur pour l'AppBar
const Color buttonColor = Colors.red; // Couleur pour le bouton de déconnexion
const Color validateTextColor = Color.fromARGB(
    255, 255, 255, 255); // Couleur pour le texte du bouton de validation

// Autres styles peuvent être définis ici
